{
  "name": "react-reactive-class",
  "version": "1.0.4",
  "description": "create reactive react classes",
  "main": "lib/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/elbenvenutti/react-reactive-class.git"
  },
  "keywords": [
    "react",
    "reactjs",
    "reactive",
    "RxJS",
    "Event",
    "flux"
  ],
  "scripts": {
    "clean": "rimraf lib dist",
    "build:lib": "babel src --out-dir lib",
    "build": "yarn run build:lib",
    "lint": "eslint src",
    "prepublish": "yarn run clean && yarn run build",
    "postinstall": "yarn run prepublish",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jas Chen",
  "license": "MIT",
  "dependencies": {
    "@babel/cli": "7.1.0",
    "@babel/core": "7.1.0",
    "@babel/polyfill": "7.4.0",
    "@babel/preset-env": "7.14.1",
    "babel-eslint": "4.1.0",
    "rimraf": "^2.4.3"
  },
  "devDependencies": {
    "eslint": "^1.3.1",
    "eslint-config-airbnb": "0.0.8",
    "eslint-plugin-react": "^3.3.0",
    "react": "^0.14.0-rc1"
  }
}
